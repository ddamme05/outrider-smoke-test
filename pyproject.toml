[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[project]
name = "outrider-taxonomy-smoke"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.110",
  "pydantic>=2.0",
  "requests>=2.31",
  "sqlalchemy>=2.0",
  "pycryptodome>=3.20",
]
